const KICKOFF=new Date('2026-08-29T16:00:00Z');const ESPN='https://site.api.espn.com/apis/site/v2/sports/football/college-football';let teams=[],selected=new Set();const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function locked(){return Date.now()>=KICKOFF}function nav(){return `<header class="top"><nav class="nav"><a class="brand" href="index.html">Fatal <span>Four</span></a><span class="season">2026–27</span><div class="links"><a href="index.html">Home</a><a href="bubble.html">Make a Bubble</a><a href="rankings.html">AP Poll</a><a href="explore.html">Explore</a><a href="messages.html">Messages</a><a href="profile.html">Profile</a></div></nav></header>`}function initPage(page){document.getElementById('nav').innerHTML=nav();if(page==='bubble')initBubble();if(page==='rankings')initRankings();if(page==='explore')initExplore();if(page==='messages')initMessages();if(page==='profile')initProfile()}async function getTeams(){if(teams.length)return teams;try{const r=await fetch(ESPN+'/teams?limit=1000');const j=await r.json();const a=j.sports?.[0]?.leagues?.[0]?.teams||[];teams=a.map(x=>{const t=x.team;return{id:t.id,name:t.displayName,logo:t.logos?.[0]?.href||'',conf:t.conference?.name||'FBS'}})}catch(e){}return teams}async function initBubble(){document.getElementById('loginNotice').innerHTML='<b>Account required.</b> Sign in before submitting a bubble. This first version shows the complete selector; secure account/database persistence is the next backend step.';document.getElementById('builder').classList.remove('hidden');await getTeams();renderTeams();document.getElementById('search').oninput=renderTeams}function renderTeams(){const q=(document.getElementById('search')?.value||'').toLowerCase();document.getElementById('teams').innerHTML=teams.filter(t=>t.name.toLowerCase().includes(q)).map(t=>`<div class="team ${selected.has(t.id)?'selected':''}" onclick="toggle('${t.id}')"><img class="logo" src="${t.logo}"><div><div class="teamname">${esc(t.name)}</div><div class="meta">${esc(t.conf)}</div></div><div class="check">${selected.has(t.id)?'✓':''}</div></div>`).join('');document.getElementById('count').textContent=`${selected.size} teams`;document.getElementById('bubble').innerHTML=selected.size?[...selected].map(id=>{const t=teams.find(x=>x.id==id);return `<div class="pill"><img src="${t.logo}">${esc(t.name)}</div>`}).join(''):'<span class="muted">No teams selected.</span>'}function toggle(id){if(locked())return;selected.has(id)?selected.delete(id):selected.add(id);renderTeams()}function clearAll(){selected.clear();renderTeams()}function saveBubble(){if(!selected.size)return alert('Select at least one team.');document.getElementById('state').textContent=locked()?'Submissions are locked.':'First-version preview saved locally. Backend persistence will replace this.'}async function initRankings(){const weeks=document.getElementById('week');['Preseason','Week 1','Week 2','Week 3','Week 4','Week 5','Week 6','Week 7','Week 8','Week 9','Week 10','Week 11','Week 12','Week 13','Week 14','Final'].forEach((w,i)=>weeks.innerHTML+=`<option value="${i}">${w}</option>`);await getTeams();renderPoll();weeks.onchange=renderPoll}function renderPoll(){document.getElementById('poll').innerHTML=teams.slice(0,25).map((t,i)=>`<tr><td><b>${i+1}</b></td><td><div class="rankteam"><img src="${t.logo}">${esc(t.name)}</div></td><td>0–0</td><td>${1600-i*45}</td><td class="same">—</td></tr>`).join('')}function initExplore(){document.getElementById('lockNotice').innerHTML=locked()?'<b class="up">Unlocked.</b> Public bubbles can now be viewed, liked and followed.':'<b>Private mode.</b> Individual bubbles remain hidden until kickoff.';document.getElementById('feed').innerHTML=locked()?'<div class="bubblecard"><b>Public feed</b><p class="muted">Backend connection will populate real player bubbles, likes, follows and profiles.</p></div>':'<div class="notice">No individual bubbles are visible yet.</div>'}function initMessages(){document.querySelector('.wrap').innerHTML+='<div class="card" style="margin-top:18px"><h2>Direct messages</h2><p class="muted">Messaging unlocks after kickoff. Production will include conversations plus block/report controls.</p><div class="notice">'+(locked()?'Messaging is unlocked.':'Messaging is locked until kickoff.')+'</div></div>'}function initProfile(){document.querySelector('.wrap').innerHTML+='<div class="card"><h1>Your profile</h1><p class="muted">Username, avatar, bubble history, likes, follows and season results will live here.</p><h2>Avatar</h2><p class="muted">Choose from every D1/FBS school logo or upload an image.</p><button class="primary">Choose avatar</button></div>'}